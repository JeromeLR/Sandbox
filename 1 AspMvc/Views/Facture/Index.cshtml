@model IEnumerable<TO.TOFacture>
@*@model IEnumerable<_1_AspMvc.Models.Facture>*@

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Identifiant
        </th>
       
        <th>
            Client
        </th>
        <th>
            Montant
        </th>
        <th>
            Nom
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Identifiant)
        </td>
        <td>
            
            @Html.ActionLink(
                item.Nom,
                "listByIdFacture" ,
                "LigneCommande",                  
                 new { idFacture=item.Identifiant, idClient=item.toClient.Identifiant }, 
                 null  
            )
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Montant)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.toClient.Nom)
        </td>
        <td>
            @*@Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |*@
            @*@Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |*@
            @*@Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })*@
        </td>
    </tr>
}

</table>
